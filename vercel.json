{
    "build": {
      "env": {
        "RUSTUP_INIT": "true"
      }
    },
    "buildCommand": "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y && source $HOME/.cargo/env && pip3 install -r requirements.txt",
    "start": "streamlit run app.py"
  }
  